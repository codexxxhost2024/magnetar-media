import { EventBlockDirective } from './event-blocker.directive';

describe('EventBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new EventBlockDirective();
    expect(directive).toBeTruthy();
  });
});
